git status
git log
git add <file>
git commit -m '<message>'
git diff <file>


#BRANCHING

git branch <branchname>   #create branch
git checkout <branchname> #starts operating in branch
git -am '<message>'       #adds and commit together


#MERGING

move to parent branch then do
git merge <child_branch>

------------------- git flow ----------------

git flow init -d #defalt branch names

git flow feature start a-new-feature
git flow feature finish new-feature


----------------- remote ------------------

git remote add origin <git@github.com: stuff>
git push <repo_name> <branch-name?>
